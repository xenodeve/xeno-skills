interface:
  display_name: "Gridgeist"
  short_description: "Build product-native interfaces with clear structure"
  icon_small: "./assets/gridgeist-small.svg"
  icon_large: "./assets/gridgeist.png"
  brand_color: "#1547FF"
  default_prompt: "Use $gridgeist to design or refine this interface around its product, brand, and behavior, then verify responsive and accessible results."
policy:
  allow_implicit_invocation: true
