{
  "t4": true,
  "verify": "bash tests/hooks/run-all.sh",
  "autoMerge": false,
  "requireGreenCI": false,
  "note": "Marker that flags this as a T4 repo — the hooks fire only when this file is present, so they never touch a non-T4 repo. \"verify\" = the repo's contract suite (the fast prefix of CI job `tests`); t4-gate RUNS it before `gh pr merge` and blocks on failure. Keep in sync with .github/workflows/t4-verify.yml."
}
